# 장기렌트 전환 랜딩 — 설치 & 연동 가이드

이 폴더의 구성과, 폼 신청을 **실제로 받는 방법**을 정리했습니다.

## 파일 구성

| 파일 | 설명 |
|---|---|
| `index.html` | **진입 셸**. 모바일은 `landing.html`로 자동 이동, PC는 모바일 폭(390px) 폰 프레임으로 미리보기. UTM·앵커 그대로 전달 |
| `landing.html` | **랜딩페이지 본체** (HTML+CSS+JS 한 파일). 폼 연동값(`LEAD_CONFIG`)은 이 파일 하단에 있음 |
| `apps-script.gs` | 구글시트로 신청을 받을 때 쓰는 Apps Script 코드 |
| `연동-가이드.md` | 이 문서 |

> **PC = 모바일 화면**: PC 폭만 줄이면 CSS 미디어쿼리(뷰포트 기준) 때문에 데스크톱 스타일이 적용됩니다.
> 그래서 PC에서는 `index.html`이 모바일 폭 iframe으로 `landing.html`을 감싸 **모바일과 100% 동일하게** 보여줍니다.
> 모바일 사용자(광고 유입)는 셸을 거치지 않고 `landing.html`로 바로 이동하므로 iframe 오버헤드가 없습니다.

> 지금 상태 그대로 열어도 **동작은 합니다.** 연동값을 넣기 전에는 폼 제출 시
> 브라우저 콘솔(F12)에 신청 데이터가 `[LEAD · mock submit]` 로 찍히고 완료 화면이 뜹니다.
> 실제로 DB에 저장하려면 아래 중 **하나**를 설정하세요.

---

## 방법 A. 구글시트 연동 (가장 간단, 추천)

1. 구글 스프레드시트 새로 만들기 (예: "장기렌트 전환 신청 DB")
2. 메뉴 **확장 프로그램 → Apps Script**
3. 기존 코드 지우고 `apps-script.gs` 내용 전체 붙여넣기
4. **배포 → 새 배포 → 유형: 웹 앱**
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자**
5. 배포 후 나오는 `https://script.google.com/macros/s/.../exec` 주소 복사
6. `landing.html` 하단의 `LEAD_CONFIG` 에서 이 줄을 채우기:

```js
window.LEAD_CONFIG = {
  SHEET_WEBAPP_URL: "https://script.google.com/macros/s/여기에_붙여넣기/exec",
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
  SUPABASE_FUNCTION: "submit-lead"
};
```

→ 끝. 폼 제출하면 시트에 한 줄씩 자동 기록됩니다. (UTM 유입정보 포함)

> ⚠️ 구글시트 방식은 `no-cors` 로 전송하므로, 브라우저에서는 성공/실패 응답을
> 직접 받지 못하고 **항상 완료 처리**됩니다. 실제 기록 여부는 시트에서 확인하세요.
> 테스트 제출 1건 → 시트에 들어오는지 꼭 확인할 것.

---

## 방법 B. Supabase 연동 (관리자 페이지·IP차단 등 확장 시)

1. Supabase 프로젝트 생성 → SQL Editor에서 `leads` 테이블 생성
   (이름·연락처·차량정보·UTM 컬럼. 스키마 예시는 페이백 예제의 `schema.sql` 참고)
2. Edge Function `submit-lead` 배포 (요청 body를 `leads` 에 insert)
3. `landing.html` 의 `LEAD_CONFIG` 채우기:

```js
window.LEAD_CONFIG = {
  SHEET_WEBAPP_URL: "",
  SUPABASE_URL: "https://xxxxx.supabase.co",
  SUPABASE_ANON_KEY: "eyJhbGci...",   // anon public 키 (공개돼도 안전)
  SUPABASE_FUNCTION: "submit-lead"
};
```

폼이 보내는 필드명: `name, phone, car, monthly_fee, remaining, switch_intent,
expected_penalty, rent_company, hope_car, contact_time, privacy,
utm_*, referrer, landing_url, user_agent, submitted_at`

---

## 이미지 교체

랜딩 곳곳에 **점선 이미지 자리(placeholder) 박스**를 넣어두었습니다. 실제 사진이 준비되면
해당 `<div class="img-ph">…</div>` 블록을 통째로 아래처럼 바꾸면 됩니다(주석으로도 표시해 둠):

```html
<img src="images/hero-car.jpg" alt="신차 SUV" class="img-real">
```

| 위치 | 권장 이미지 | 권장 사이즈 | 파일명(예) |
|---|---|---|---|
| Hero 우측 | 국산 SUV·세단 정면/측면 (밝고 신뢰감) | 1200×900 | `images/hero-car.jpg` |
| Insight | 차량 키·계약서를 두고 고민하는 장면 | 1000×750 | `images/insight.jpg` |
| Service | 상담 장면 / 스마트폰으로 안내받는 장면 | 1200×525 | `images/consult.jpg` |

> 이미지는 `images/` 폴더에 넣고, `.img-real` 클래스를 주면 라운드·그림자가 자동 적용됩니다.
> 사진을 안 넣어도 점선 박스가 깔끔하게 표시되므로 지금 상태로도 배포 가능합니다.

---

## 배포(호스팅)

`index.html` 1개만 올리면 됩니다. (이미지 의존성 없음)
- Vercel / Netlify / Cloudflare Pages / 가비아 웹호스팅 등 어디든 가능
- Vercel: 이 폴더에서 `vercel --prod`

---

## 광고 전환 추적 (선택)

폼 제출이 성공하면 자동으로 아래가 호출됩니다 (설치돼 있을 때만):
- Google Ads / GA4: `gtag('event','generate_lead')`
- Meta(페이스북) 픽셀: `fbq('track','Lead')`
- 커스텀: `window.onLeadSubmitted(data)` 함수를 정의하면 신청 데이터로 호출됩니다.

`<head>` 에 GA/Meta 픽셀 기본 스니펫만 넣으면 전환 이벤트가 바로 잡힙니다.

---

## 교체해야 할 placeholder (배포 전 체크)

`landing.html` 에서 `TODO` 로 표시해 둔 곳:
- [ ] 헤더/푸터 **브랜드명** ("장기렌트 전환케어")
- [ ] 푸터 **사업자 정보** (상호·대표·사업자등록번호·주소·이메일)
- [ ] 푸터 **대표 전화번호** (`tel:` 링크)
- [ ] 푸터 **개인정보처리방침** 링크
- [ ] `LEAD_CONFIG` 전송 주소 (방법 A 또는 B)
